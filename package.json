{
  "name": "duplicated-properties-checker",
  "version": "0.1.0",
  "description": "Returns all property keys which are not unique",
  "main": "dist/index.js",
  "scripts": {
    "test": "test",
    "dist": "standard ./src/*.js && ./node_modules/.bin/babel src --out-dir build",
    "start": "node ./build/index"
  },
  "keywords": [
    "JSON",
    "duplicated",
    "properties"
  ],
  "author": "SebastianG",
  "license": "MIT",
  "dependencies": {
    "@babel/polyfill": "^7.0.0",
    "is-json": "^2.0.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.1",
    "@babel/preset-env": "^7.0.0",
    "standard": "^12.0.1"
  }
}
